# Zig Shell

**Goal**: The project goal is to learn zig language

## Build and compile

To build a project run `zig build`  
To run a project run `zig build run`  
To test a project run `zig build test`  
